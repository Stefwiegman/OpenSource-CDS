# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Virtuele omgevingen
.venv/
venv/
env/

# Gegenereerde meetdata + presets (gebruikersstate, hoort niet in repo)
data/
calibration.yaml
scan_presets.yaml
confocal_fits.yaml

# Gridsearch-output (regenereerbaar)
gridsearch_results.csv

# CAD-exports (de STEP-bronnen in cad/step/ zijn bewust wel getrackt)
*.gltf
*.glb

# Eigen meetdata + plots (onze metingen, niet relevant voor anderen)
[Mm]etingen*.xlsx
/*mm.png

# Interne paper- en design-artefacten (lokaal behouden, niet publiek)
paper_overlay.py
assets/overlay_predictions.png
mockup.html

# Geuploade archieven (inhoud wordt uitgepakt, de zip zelf niet committen)
*.zip